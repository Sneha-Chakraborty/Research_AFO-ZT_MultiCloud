"""Cross-Cloud Trust Graph (Step 2.3)

SQLite-backed storage + incremental updates.

The trust graph links identities, devices, roles, workloads, resources, tenants, regions, and clouds across providers.
"""
